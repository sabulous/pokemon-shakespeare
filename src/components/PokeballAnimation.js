import React from "react";
import pokeball from "../pokeball.gif";

const PokeballAnimation = (props) => {
  return (
    <img src={pokeball} alt="pokeball gif" className="pokeball-animation" />
  );
};

export default PokeballAnimation;
